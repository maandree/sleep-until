RUNTIME DEPENDENCIES:

	linux>=2.6.25
	libc (glibc>=2.8)


BUILD DEPENDENCIES:

	libc (glibc>=2.8)
	c99
	make
	coreutils
	sed
	auto-auto-complete (opt-out, for shell tab-completion)


INSTALL DEPENDENCIES:

	make
	coreutils
