RUNTIME DEPENDENCIES:

	linux>=2.6.25
	libc (glibc>=2.8)


BUILD DEPENDENCIES:

	libc (glibc>=2.8)
	cc
	make
	coreutils
	grep
	sed
	auto-auto-complete (out-put, for shell tab-compeltion)


INSTALL DEPENDENCIES:

	make
	coreutils
